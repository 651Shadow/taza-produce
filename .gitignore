# dependencies
node_modules/

# next.js build output
/.next/
/out/
next-env.d.ts

# env / secrets — keep the template, ignore real env
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# verification artifacts (headless screenshots, diff dirs)
shot_*.png
ud_*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# build / deps
node_modules/
.next/
out/

# env / secrets — never commit real values
.env
.env*.local
.env.local

# static-mockup tooling (not part of the Next app)
mockup/
*.ps1
clean_header.py
transform.py

# TS incremental build info
tsconfig.tsbuildinfo
*.tsbuildinfo

# playwright MCP scratch
.playwright-mcp/
*.png.bak

.vercel

# Private project report (build/status/architecture notes) — for the dev only,
# never deployed to the public site or committed to the shared repo.
REPORT/
